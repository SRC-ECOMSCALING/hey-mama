# Loaded only by `vite build` (production mode): the native/production client
# calls the deployed backend. Dev uses same-origin (no base URL).
VITE_API_BASE_URL="https://hey-mama-production.up.railway.app"
